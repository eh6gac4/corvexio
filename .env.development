# Used automatically by `next dev` (and by scripts/mock-obsidian-server.ts via `npm run dev:mock`).
# Points at the local mock Local REST API server, not a real vault, so it's safe to commit.
OBSIDIAN_API_URL=https://127.0.0.1:27124
OBSIDIAN_API_KEY=dev-mock-api-key
OBSIDIAN_TLS_INSECURE=true
